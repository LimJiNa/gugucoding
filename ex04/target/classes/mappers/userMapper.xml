<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace ="com.jinalim.admin.mappers.userMapper">

	<select id="login" resultType="UserVO">
		select userid, userpw, username from tbl_users hwere userid = #{userid} and userpw = #{userpw}
	</select>

</mapper>